Collected env-like keys, secrets, tokens, and URLs found under admin_app

File: admin_app/lib/src/core/constants/google_auth_defaults.dart
- POS_GOOGLE_WEB_CLIENT_ID (from environment):
  static const String webClientId = String.fromEnvironment(
    'POS_GOOGLE_WEB_CLIENT_ID',
    defaultValue:
        '17506890816-phe8hk3uo1ia5tjvaqqt06s7inu43v29.apps.googleusercontent.com',
  );

File: admin_app/lib/src/core/constants/dev_auth_defaults.dart
- STAFF_DEV_BYPASS_SECRET mention / POS_STAFF_DEV_BYPASS env flag:
  // Optional dev staff login without Google (server must set STAFF_DEV_BYPASS_SECRET).
  bool.fromEnvironment('POS_STAFF_DEV_BYPASS', defaultValue: false);

File: admin_app/lib/src/services/connectivity_service.dart
- Google-related hosts and probe URLs used by the app:
  'accounts.google.com'
  'oauth2.googleapis.com'
  'www.googleapis.com'
  Uri.parse('https://connectivitycheck.gstatic.com/generate_204')
  Uri.parse('https://www.google.com/generate_204')

File: admin_app/android/key.properties.example
- Keystore placeholders (storePassword/keyPassword):
  storePassword=CHANGE_ME
  keyPassword=CHANGE_ME

File: admin_app/android/app/src/main/kotlin/com/terabyteAI/Res/Admin/MainActivity.kt
- Facebook login config values passed from Dart to native code:
  val appId = call.argument<String>("appId")?.trim().orEmpty()
  val clientToken = call.argument<String>("clientToken")?.trim().orEmpty()
  FacebookSdk.setApplicationId(appId)
  FacebookSdk.setClientToken(clientToken)
  // Access token returned on success:
  val token = result.accessToken.token

Notes:
- I excluded matches from build/ and generated files; only source-relevant items are listed above.
- If you want I can also:
  - scan other top-level folders the same way, or
  - redact or move these entries to a secure vault, or
  - run a stricter scan for API key formats (long base64-like strings).
