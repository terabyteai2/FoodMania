fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.15
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0
python-multipart>=0.0.20
pydantic[email]>=2.0
python-dotenv>=1.0
aiofiles>=24.1
pyngrok>=7.2
pydantic-settings>=2.0
boto3>=1.34
gunicorn>=21.2
google-auth>=2.35
requests>=2.32
httpx>=0.27
sentry-sdk[fastapi]>=2.0
# CentOS 7 VPS has GCC 4.8; NumPy 2.3 falls back to a source build there.
numpy<2.3
# Pandas 3 likewise drops the wheel path used by the current VPS baseline.
pandas<3
# ujson 5.11+ falls back to a source build on the current VPS baseline.
ujson<5.11
paddleocr>=3.0
paddlepaddle>=3.0
pytest>=8.0
pytest-asyncio>=0.24
