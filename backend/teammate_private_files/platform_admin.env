# Platform admin — API backend URL
# Points to the VPS server so the panel works without a local backend.
# For local development against a local backend, comment this out.
VITE_API_BASE_URL=https://160-187-130-80.sslip.io
